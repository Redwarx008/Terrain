# 修复材质槽侧栏缩略图与无意义槽位占位符

## 目标

- 恢复右侧 biome 材质面板中的纹理缩略图显示。
- 去掉 biome layer 列表和材质选择区域里无意义的 `Slot {n}` 占位式文案。

## 验收标准

- 选中 biome layer 时，右侧 `MATERIAL` 卡片能显示当前材质槽的 albedo 缩略图。
- 当没有可用缩略图时，面板仍显示原有图标占位，不出现空白报错。
- biome layer 列表项不再显示 `Slot 0` 之类的 badge。
- 材质下拉和标题优先显示材质名；未分配时显示明确的空状态文案，而不是槽位编号。
